const express = require('express');
const router = express.Router();

// Placeholder for order routes
router.get('/', (req, res) => {
  res.json({ message: 'Order routes placeholder' });
});

module.exports = router;